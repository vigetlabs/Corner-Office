<%= simple_form_for :session, :url => session_path do |f| %>
  <%= f.input :email %>
  <%= f.input :password %>
  <%= f.button :submit, :value => "Submit" %>
  <%= link_to "Sign Up for an Account", new_account_path %>
<% end %>